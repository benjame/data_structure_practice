#include <cstdio>
#include <cstdlib>
#include <cstring>






void AddPolyn(PLinkList &Pa, PLinkList &Pb) {
    PLink ha, hb, qa, qb;
    PElemType a, b, temp;
    float sum;
    ha = GetHead(Pa);
    hb = GetHead(Pb);
    
}